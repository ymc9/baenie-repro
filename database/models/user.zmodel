import './space'
import './membership'
import '../schema'

model User {
    id          Int          @id @default(autoincrement())
    spaces      Space[]
    memberships Membership[]
    posts       Post[]
}
